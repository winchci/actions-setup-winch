# setup-winch

Setup winch GitHub Action

Example:

```yaml
- uses: winchci/setup-winch@v1
```

If you want to use a specific version of winch:

```yaml
- uses: winchci/setup-winch@v1
  with:
    version: 1.29.5
```
